# hypha-whisper-node dependencies
# Jetson Orin Nano — JetPack 6.2 (L4T R36.5), CUDA 12.6, Python 3.10
#
# Step 1 — system packages (requires sudo):
#   sudo apt-get install -y python3-pip python3-venv portaudio19-dev ffmpeg
#
# Step 2 — PyTorch from NVIDIA JetPack 6.2 wheel (pick latest cp310 wheel):
#   pip install https://developer.download.nvidia.com/compute/redist/jp/v62/pytorch/<wheel>.whl
#
# Step 3 — install the rest:
#   pip install -r requirements.txt

openai-whisper==20250625
SpeechRecognition==3.10.4
numpy==1.26.4
tiktoken==0.7.0
webrtcvad==2.0.10
pyyaml==6.0.3
hypha-rpc
pyaudio
