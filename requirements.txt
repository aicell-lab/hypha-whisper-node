# hypha-whisper-node dependencies
# Jetson Orin Nano — JetPack 6.2 (L4T R36.5), CUDA 12.6, Python 3.10
#
# Step 1 — system packages (requires sudo):
#   sudo apt-get install -y python3-pip python3-venv portaudio19-dev ffmpeg
#
# Step 2 — PyTorch from NVIDIA JetPack 6.1 wheel (JP6.2 wheel not yet published; JP6.1 works on CUDA 12.6):
#   pip install https://developer.download.nvidia.com/compute/redist/jp/v61/pytorch/torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl
#
#   libcusparseLt is missing on JP6.2 — extract from NVIDIA sbsa repo and add to LD_LIBRARY_PATH:
#   wget https://developer.download.nvidia.com/compute/cuda/repos/ubuntu2204/sbsa/libcusparselt0_0.6.3.2-1_arm64.deb
#   mkdir -p ~/.local/lib && ar x libcusparselt0_0.6.3.2-1_arm64.deb && tar -xf data.tar.xz
#   cp usr/lib/aarch64-linux-gnu/libcusparseLt.so.0* ~/.local/lib/
#   echo 'export LD_LIBRARY_PATH="$HOME/.local/lib:/usr/local/cuda/lib64${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"' >> ~/.bashrc
#
# Step 3 — install the rest:
#   pip install -r requirements.txt

openai-whisper==20250625
faster-whisper>=1.0.3           # default ASR backend; uses CTranslate2 (no scipy dependency)
whisper-timestamped>=1.15.4     # alternative backend (--backend whisper-timestamped)
numpy==1.26.4                   # pinned: whisper-timestamped upgrades to 2.x which breaks PyTorch + scipy
tiktoken==0.7.0
librosa>=0.10.0                 # required by whisper_online.py
pyyaml==6.0.3
hypha-rpc==0.21.31
pyaudio==0.2.14
fastapi==0.115.12
uvicorn==0.34.0
